{
  "name": "cpx-upload-worker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "sync-cases": "node scripts/sync-cases.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
